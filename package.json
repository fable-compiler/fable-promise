{
  "private": true,
  "scripts": {
    "build": "fable Build.fsx --run",
    "pretest": "fable-splitter tests -o build/tests --commonjs",
    "test": "mocha build/tests -t 10000"
  },
  "dependencies": {
    "fable-compiler": "^2.8.1",
    "fable-compiler-js": "^1.3.1",
    "fable-publish-utils": "^1.2.1",
    "fable-splitter": "^2.1.13",
    "mocha": "^7.1.1"
  },
  "devDependencies": {
    "npm-check-updates": "^4.1.2",
    "npx": "^10.2.2"
  }
}
